apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: carts-db-temp-pvc
  namespace: sock-shop
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard

  #storageClassName: nfs-client (bch nstaamlou flminikube standard storage pour le moment lihoua default )