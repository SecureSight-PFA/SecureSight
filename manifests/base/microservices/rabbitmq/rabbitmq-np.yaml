apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rabbitmq-access
  namespace: sock-shop
spec:
  podSelector:
    matchLabels:
      name: rabbitmq
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              name: shipping
        - podSelector:
            matchLabels:
              name: queue-master
        - podSelector:
            matchLabels:
              name: orders
      ports:
        - protocol: TCP
          port: 5672