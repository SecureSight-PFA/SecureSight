apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: shipping-access
  namespace: sock-shop
spec:
  podSelector:
    matchLabels:
      name: shipping
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              name: orders
        - podSelector:
            matchLabels:
              name: front-end
      ports:
        - protocol: TCP
          port: 80