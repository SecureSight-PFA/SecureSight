---
apiVersion: v1
kind: Service
metadata:
  name: catalogue
  annotations:
    prometheus.io/scrape: "true"
  labels:
    name: catalogue
  namespace: sock-shop
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    name: catalogue
