apiVersion: v1
kind: Service
metadata:
  name: front-end
  namespace: sock-shop
  labels:
    name: front-end
    monitor: "true"
spec:
  selector:
    name: front-end
  ports:
    - name: http
      port: 80
      targetPort: 8079
      protocol: TCP