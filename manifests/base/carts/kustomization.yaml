apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: sock-shop
  namespace: sock-shop

sortOptions:
  order: fifo

resources:
  - carts-dep.yaml
  - carts-svc.yaml
  - carts-db-statefulset.yaml  
  - carts-db-svc.yaml
  - carts-hpa.yaml