apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: 
  - ../../base
#well later add ebs storageclass and patch storage statefulsets

patches: 
  - path: ingress-patch.yaml #replace nginx ingress with alb ingress

secretGenerator:
  - name: mongodb-creds
    namespace: sock-shop
    envs:
      - env.secret
